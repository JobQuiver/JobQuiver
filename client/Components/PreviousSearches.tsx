import React, { FC } from 'react';
import PreviousSearch from './PreviousSearch';

const PreviousSearches: FC<any> = (props: any) => {
  return (
    <div>
      <PreviousSearch />
    </div>
  );
};

export default PreviousSearches;
